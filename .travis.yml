os: linux
dist: bionic
git:
  depth: false
language: python
python:
    - "3.5"
    - "3.6"
    - "3.7"
    - "3.8"
    # - "pypy3" # out of date OpenSSL error

jobs:
    include:
        - python: 2.7
          dist: trusty

install:
    - pip install tox-travis

script:
    - tox
